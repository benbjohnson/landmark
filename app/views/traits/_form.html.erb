<%= form_tag(@trait.id == 0 ? traits_path : trait_path(@trait.name), :method => @trait.id == 0 ? 'POST' : 'PUT') do %>
  <div class="control-group">
    <%= label_tag :name, nil, :class => 'control-label' %>
    <div class="controls">
      <%= text_field_tag :name, @trait.name %>
    </div>
  </div>

  <% if @trait.id == 0 %>
    <div class="control-group">
      <%= label_tag :data_type, nil, :class => 'control-label' %>
      <div class="controls clearfix">
        <div class="row">
          <div class="span3">
            <select name="large" class="select-block mbl">
              <option value="factor">Categorical</option>
              <option value="string">String</option>
              <option value="integer">Integer</option>
              <option value="float">Decimal</option>
              <option value="boolean">True/False</option>
            </select>
          </div>
        </div>
      </div>
    </div>
  <% end %>
  
  <div class="control-group">
    <div class="controls">
      <%= submit_tag(@trait.id == 0 ? 'Create Trait' : 'Update Trait', :class => 'btn btn-primary btn-embossed') %>
      <%= link_to('Back', :back, :class => 'btn btn-inverse btn-embossed') %>
    </div>
  </div>
<% end %>
