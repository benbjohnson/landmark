<div class="row" style="padding: 0 32px; margin-top:20px">
  <div class="span12">
    <% if @properties.length > 0 %>
      <%= link_to('Add Property', new_property_path, :class => 'btn btn-small btn-primary btn-embossed mlm pull-right', :style => 'margin-top:10px')%>
    <% end %>

    <h3>Action Properties</h3>

    <% if @properties.length == 0 %>
      <p>It doesn't look like you're tracking any properties about your user's actions yet.</p>
      <p>These properties are great for tracking things like the purchase price of a checkout.</p>

      <%= link_to('Add Property', new_property_path, :class => 'btn btn-large btn-primary btn-embossed mlm', :style => 'margin-top:10px')%>
    <% else %>
      <p>Track data about the actions your users perform.</p>

      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>Property Name</th>
            <th>Data Type</th>
          </tr>
        </thead>
        <tbody>
          <% @properties.each do |property| %>
            <tr>
              <td><%= property.name %></td>
              <td><%= data_type_label(property.data_type) %></td>
              <td><%= link_to('Delete', property_path(property.name), :method => 'DELETE', :confirm => "Are you sure you want to delete '#{property.name}'?") %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  </div>
</div>
